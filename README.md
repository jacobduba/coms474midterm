# coms474midterm

Install UV:
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

Create venv:
```bash
uv venv
source .venv/bin/activate
uv pip -r requirements.txt
```

Run:

```bash
uv run analysis.py
```
